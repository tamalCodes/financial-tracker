import MobileHome from "@/features/dashboard/mobile/MobileHome";

// TEMP preview route (no auth) for visual QA of the demo-data mobile home.
export default function DemoPage() {
  return <MobileHome />;
}
